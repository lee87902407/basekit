# 示例代码目录

本目录用于存放各主模块的示例代码。

## 约定

新增主模块时，应同步新增或更新对应目录：

- `examples/<module>/`

建议每个模块的示例代码做到：

1. 可以独立阅读。
2. 与示例文档中的最小示例保持一致。
3. 在模块接口变化时同步调整。

## 当前状态

当前仓库已引入正式主模块示例代码：

- `examples/mempool/`：展示 `mempool` 的最小可运行用法。
- `examples/log/`：展示 `log` 模块的最小可运行初始化与输出方式。
